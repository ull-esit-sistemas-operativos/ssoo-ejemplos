if(UNIX)
    add_executable(mqueue-control mqueue-control.cpp)
    target_link_libraries(mqueue-control rt)
    add_executable(mqueue mqueue.cpp ${PROJECT_SOURCE_DIR}/src/common/timeserver.cpp)
    target_link_libraries(mqueue rt)
endif()
